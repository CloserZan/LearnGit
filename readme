git仓库 提交顺序: 工作空间 --git add--> 暂存区 --git commit--> 本地仓库 --git push--> 远程仓库
       拉取顺序: 工作空间 <--git restore-- 暂存区

git命令: git status   查看文件状态
        git log --oneline   查看提交记录和版本号
        git branch '分支名'     创建分支命令
        git checkout '分支名'  切换分支命令
        git branch  查看当前有哪些分支

1.小版次更改可以使用 'git add 要添加的文件'命令,回滚使用 'git restore 要回滚的文件'命令
2.git可以忽略文件,在 gitignore 文件中进行配置